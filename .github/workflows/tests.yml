name: Tests

on:
  push:
  pull_request:
  workflow_dispatch:

# Ein neuer Push bricht den noch laufenden Lauf desselben Zweigs ab.
concurrency:
  group: tests-${{ github.ref }}
  cancel-in-progress: true

jobs:
  tests:
    name: ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    timeout-minutes: 40
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest]
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: ExifTool und Qt-Grundlagen installieren (Linux)
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y libimage-exiftool-perl libegl1 libgl1 libxkbcommon0 libfontconfig1 libdbus-1-3
          exiftool -ver

      # Unter Windows dasselbe ExifTool wie im Paket, und die Tests starten es
      # wie das Paket: perl.exe mit exiftool.pl direkt (metadaten.exiftool_befehl).
      - name: ExifTool holen (Windows, wie im Paket)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          python paket/exiftool_holen.py build/exiftool
          "$((Resolve-Path build/exiftool).Path)" | Out-File -FilePath $env:GITHUB_PATH -Encoding utf8 -Append

      - name: Abhaengigkeiten installieren
        run: |
          python -m pip install --upgrade pip
          python -m pip install -e ".[test]" pytest-timeout

      - name: Tests
        # Haengt ein Test, bricht er nach 5 Minuten mit Stapelabzug ab,
        # statt den Lauf stundenlang zu blockieren. Das Qt-Fenster laeuft ohne Anzeige.
        env:
          QT_QPA_PLATFORM: offscreen
        run: python -m pytest -q -p no:cacheprovider --timeout=300 -o faulthandler_timeout=290
